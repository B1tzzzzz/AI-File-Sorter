[project]
name = "venv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "B1tzzzzz", email = "matew651@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.4.2",
    "langgraph>=1.2.12",
]

[project.scripts]
venv = "venv:main"

[build-system]
requires = ["uv_build>=0.12.19,<0.13.0"]
build-backend = "uv_build"
